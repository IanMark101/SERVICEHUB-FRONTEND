import { Trash2 } from 'lucide-react';

interface AccountDangerZoneProps { isOpen: boolean; confirmation: string; deleting: boolean; cardBg: string; innerBg: string; headingText: string; labelText: string; inputClass: string; onOpen: () => void; onClose: () => void; onConfirmationChange: (value: string) => void; onDelete: () => void; }

export default function AccountDangerZone({ isOpen, confirmation, deleting, cardBg, innerBg, headingText, labelText, inputClass, onOpen, onClose, onConfirmationChange, onDelete }: AccountDangerZoneProps) {
  return <><div className="bg-rose-500/5 border border-rose-500/20 rounded-[24px] p-6 space-y-3"><h3 className="font-black text-sm uppercase tracking-wider text-rose-500 flex items-center gap-2"><Trash2 size={17} /> Danger Zone</h3><p className="text-xs text-rose-400/90 leading-relaxed">Deleting your ServiceHub account will permanently erase your profile, booking records, trust score, and message history. This action cannot be undone.</p><button type="button" onClick={onOpen} className="px-4 py-2.5 rounded-xl text-xs font-bold bg-rose-600 hover:bg-rose-700 text-white transition-all shadow-sm active:scale-95">Delete Account</button></div>{isOpen && <div className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-center justify-center p-4"><div className={`${cardBg} max-w-md w-full rounded-3xl p-6 border shadow-2xl space-y-4`}><div className="w-12 h-12 rounded-2xl bg-rose-500/10 border border-rose-500/20 text-rose-500 flex items-center justify-center"><Trash2 size={24} /></div><h4 className={`text-base font-black ${headingText}`}>Confirm Account Deletion</h4><p className={`text-xs ${labelText} leading-relaxed`}>Type <strong className="text-rose-500">DELETE</strong> below to permanently erase your ServiceHub profile and account data.</p><input type="text" value={confirmation} onChange={(event) => onConfirmationChange(event.target.value)} placeholder="Type DELETE" className={inputClass} /><div className="flex gap-2 pt-2"><button type="button" onClick={onClose} className={`flex-1 py-2.5 rounded-xl text-xs font-bold border ${innerBg} ${headingText}`}>Cancel</button><button type="button" disabled={confirmation !== 'DELETE' || deleting} onClick={onDelete} className="flex-1 py-2.5 rounded-xl text-xs font-bold bg-rose-600 hover:bg-rose-700 text-white transition-all disabled:opacity-50">{deleting ? 'Deleting...' : 'Permanently Delete'}</button></div></div></div>}</>;
}
